# Khadamny Workspace Instructions

- Phase 1 focus: React frontend + Flask backend + SQLite.
- Keep changes minimal and aligned with the MVP.
- Frontend lives in `frontend/` and backend in `backend/`.
- The main flow is upload PDF -> extract text -> show the result in the dashboard.
